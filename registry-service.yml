eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com.netflix.eureka: 'OFF'
    com.netflix.discovery: 'OFF'

server:
  port: 8761

management:
  server:
    port: 8083

---
spring:
  profiles: cloud

server:
  port: 8080

management:
  server:
    port: 8081

